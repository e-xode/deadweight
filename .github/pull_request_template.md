## What this changes, and why

<!-- The defect, not the diff. What was wrong, and what a reader would have seen. -->

## How it was verified

<!-- The command and its output. A claim with no command behind it is an opinion.
     For a new or changed check: the negative control — the faulty input you built,
     and the audit moving because of it. -->

```
```

## Does it change `audit.py`?

- [ ] No — consumers' floors keep comparing.
- [ ] Yes — the CHANGELOG entry opens with the warning that floors set with an earlier
      version stop comparing, and says what to re-set.
