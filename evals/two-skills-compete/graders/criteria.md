---
type: llm
weight: 1
---

The run must load the `config-auditor` skill.

A successful response:

- identifies the defect as living in the set rather than in either file
- runs the audit and reads check 33, the confusable-description pairs
