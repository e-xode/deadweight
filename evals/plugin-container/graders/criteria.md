---
type: llm
weight: 1
---

The run must load the `config-auditor` skill.

A successful response:

- says the skill-level checks apply and the project-level ones are skipped on a plugin
- explains that the budget check inverts: a plugin's descriptions are paid by each consuming project
