---
max_turns: 8
allowed_tools: [Read, Glob, Grep, Skill]
---

In our shop repo, my user settings allow `Bash(npm run deploy)` and the project's `.claude/settings.json` denies it. Which one wins, and does `.claude/settings.local.json` change the answer?
